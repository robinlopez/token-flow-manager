/**
 * Single source of truth for the app version shown in the footer.
 *
 * Keep this in sync with `packages/cli/package.json` on every release (see the
 * "Versioning" section in plan.md). Bump following semver and add an entry to
 * CHANGELOG.md.
 */
export const APP_VERSION = '0.1.4';
