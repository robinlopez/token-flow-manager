node_modules/
dist/
examples/
.tokenflow/
*.log
.DS_Store
coverage/
.angular/
packages/web/dist/
packages/cli/standalone/
src-tauri/target/

# Zensical docs build output + local Python venv
/site/
.venv/
.zensical/

# Local throwaway test projects (manual distribution / config testing)
sandbox/
# Build script generated by the tool's own "Save build" when tested against this repo
/scripts/tokens.build.mjs

# GitHub release notes (drafted locally, pasted into the GitHub Releases UI)
/RELEASE-*.md

# npm pack tarballs (release artifacts, attached to GitHub Releases)
*.tgz

# Editor / local agent config (keep private paths out of the public repo)
.idea/
.claude/
